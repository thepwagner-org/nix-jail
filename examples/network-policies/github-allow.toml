# Allow access to GitHub with credential injection

[[rules]]
action = "allow"
credential = "github"
[rules.pattern]
host = "api\\.github\\.com"

[[rules]]
action = "allow"
credential = "github"
[rules.pattern]
host = "github\\.com"
